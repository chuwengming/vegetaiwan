---
alwaysApply: false
---

# Loop Engineering

## 啟用方式

**當使用者輸入 `@loop-engineering`，或明確要求「Loop Engineering 模式」時，本規則生效。**

此時必須進入 Loop Engineering 工作流；未 @ 提及、也未明確要求時，**不要**自動套用本流程，維持一般詢問方式即可。

## 適用 vs 不適用

**適用（需 Goal Contract + 迭代）：**
- 修 bug、不確定根因
- 新功能、需驗證才算完成
- 需跑 test / lint / build
- 可能多輪才成功

**不適用（一般詢問即可）：**
- 解釋程式碼
- 小改動、一次可完成
- 純問答、不需驗證迴圈

## 開始前：Goal Contract

動手改 code 前，先列出 goal contract：

- 可量化的完成條件
- 明確的檢查方式
- 限制與禁止事項
- 最大迭代輪數或時間上限

未完成 goal contract 前，不要開始編輯程式碼。

## 每一輪必須包含

1. 本輪假設
2. 最小必要修改
3. 執行驗證指令
4. 結果摘要
5. 若失敗，記錄錯誤原因
6. 下一輪依新證據調整策略

## Vegetaiwan 專案驗證指令

依任務性質，每輪至少執行以下其中一項：

- `npm run lint` — 程式碼風格與 ESLint 檢查
- `npm run build` — Next.js 建置是否成功
- 若涉及 UI，確認 dev server 下頁面可正常渲染

未執行檢查就宣稱完成，視為未完成。

## 限制

- 不得在沒有新證據時重複同一失敗策略
- 不得超出任務範圍做無關 refactor
- 不得修改 `.next/` 等建置產物
- 遇到產品或架構決策 blocker 時，停止並詢問使用者

## 錯誤記憶運作程序

1. **任務開始前**：讀取 `docs/agent-memory/loop-failures.md`，查看是否有類似失敗可參考。
2. **每輪檢查失敗後**：立即追加一筆，不可延後到任務結束。
3. **同一任務多輪失敗**：追加在同一任務區塊下。
4. **下一輪開始前**：說明如何依上一輪失敗調整策略。
5. **任務成功後**：將該任務標記為「已解決」，並記錄有效做法。
6. **連續 2 輪相同原因失敗**：停止並向使用者回報 blocker。

每筆記錄包含：任務名稱、失敗假設、錯誤訊息、失敗原因、下一輪調整方向。

## 停止條件

符合以下任一條件即停止：

- 所有完成條件已達成
- 達到迭代或時間上限
- 遇到需使用者決策的 blocker

## 結束摘要

任務結束時提供：

- 最終狀態
- 驗證證據（指令輸出或檢查結果）
- 殘留風險
- 可供後續參考的 lessons learned
